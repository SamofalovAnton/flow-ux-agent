import React from 'react'
import ReactDOM from 'react-dom/client'
import FlowAgent from './FlowAgent.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <FlowAgent />
  </React.StrictMode>,
)
